<form method="post" action="{{ path('app_demarchage_delete', {'id': demarchage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demarchage.id) }}">
    <div class="d-grid gap-2 pt-2">
        <button class="btn btn-block btn-danger">Delete</button>
    </div>
</form>
