<form method="post" action="{{ path('app_bloccv_delete', {'id': bloccv.id}) }}" onsubmit="return confirm('Être vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bloccv.id) }}">
    <div class="d-grid gap-2 pt-2">
    <button class="btn btn-block btn-danger">Effacer</button>
    </div>
</form>
